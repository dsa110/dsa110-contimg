events { worker_connections 1024; } http { upstream pipeline { server pipeline:8080; } upstream monitoring { server monitoring:9090; } server { listen 80; location / { proxy_pass http://pipeline; } location /metrics { proxy_pass http://monitoring; } } }
