# API Configuration
# Set explicit API URL to avoid proxy issues when accessing from different contexts
VITE_API_URL=http://localhost:8000/api

# CARTA Integration Configuration
# CARTA Backend WebSocket URL (default: ws://localhost:9002)
# For production, use the actual CARTA backend hostname/IP
VITE_CARTA_BACKEND_URL=ws://localhost:9002

# CARTA Frontend URL (for iframe integration, default: http://localhost:9003)
# For production, use the actual CARTA frontend hostname/IP
VITE_CARTA_FRONTEND_URL=http://localhost:9003
