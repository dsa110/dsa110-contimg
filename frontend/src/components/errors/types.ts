/**
 * Re-export error types from the canonical location.
 * Components should import from here or directly from '../../types/errors'.
 */
export type { ErrorResponse, MappedError, ErrorSeverity } from "../../types/errors";
