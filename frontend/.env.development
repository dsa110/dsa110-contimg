# Development environment - use Vite proxy to avoid CORS issues
# The proxy is configured in vite.config.ts to forward /api/* to localhost:8000
# Using relative URL lets the proxy handle requests
VITE_API_URL=/api/v1
