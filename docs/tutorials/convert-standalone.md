# Tutorial: Convert UVH5 → MS (Standalone)

Run the converter directly for one-off conversions.

```
python -m dsa110_contimg.conversion.uvh5_to_ms --help
```

Example:
```
python -m dsa110_contimg.conversion.uvh5_to_ms   /path/to/input.uvh5 /path/to/output.ms
```
